<Window x:Class="StudentClinicMIS.Views.AddAppointmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Запись на приём"
        Height="340" Width="420"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel materialDesign:TextFieldAssist.TextBoxViewMargin="0"
                    materialDesign:TextFieldAssist.DecorationVisibility="Visible"
                    VerticalAlignment="Top"
                    Orientation="Vertical"
                    Margin="0,0,0,10">

            <ComboBox x:Name="DoctorComboBox"
                      Style="{StaticResource MaterialDesignFilledComboBox}"
                      DisplayMemberPath="FullName"
                      SelectedValuePath="DoctorId"
                      materialDesign:HintAssist.Hint="Выберите врача"
                      Margin="0,10,0,0"/>

            <DatePicker x:Name="DatePicker"
                        Style="{StaticResource MaterialDesignDatePicker}"
                        materialDesign:HintAssist.Hint="Дата приёма"
                        Margin="0,10,0,0"/>

            <TextBox x:Name="StartTimeBox"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="Время начала (HH:mm)"
                     Margin="0,10,0,0"/>

            <Button Content="Сохранить"
                    Click="SaveButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0"
                    Width="120"/>
        </StackPanel>
    </Grid>
</Window>
